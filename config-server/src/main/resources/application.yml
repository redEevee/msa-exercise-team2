server:
  port: 2345

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: file:///D:/proj/msa-exercise-team2/config-repo
          default-label: main