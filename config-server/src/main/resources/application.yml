server:
  port: xxxx

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: ??? ???? ?? ???? ? ??? ????/config-repo
          default-label: main