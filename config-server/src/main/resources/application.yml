server:
  port: 2345

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: ??? ???? ?? ???? ? ??? ????/config-repo
          default-label: main